# SkillBridge

Minimal Vite + React scaffold demonstrating a production-ready `Navbar` component.

Development

Install dependencies:

```bash
npm install
```

Run dev server:

```bash
npm run dev
```

Open `http://localhost:5173`.

This scaffold includes a responsive, sticky navbar in `src/components/Navbar` following the design brief.